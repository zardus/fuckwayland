# Lifted from recon/wire/trace_rr2.log connection 2, captured 2026-09-10
# against the box's own X server on :355 (a TigerVNC screen called VNC-0)
# through recon/wire/proxy.py, while `xrandr --output VNC-0 --mode 1680x1050`
# ran. RANDR major 140 there, root 0x22b, crtc 0x3a, output 0x3b, config
# timestamp 0x304a204b. It is the rig tests/fixtures/xw11/xrandr-mode-write.hex
# came off, so the two go together.
# GetCrtcInfo(0x3a): 1280x1024 at 0,0 on mode 0x42, rotation 1,
# driving output 0x3b. The mode the capture switches TO is 0x3f.
01000d000200000047204a3000000000000500044200000001000100010001003b0000003b000000
