# w11: load uinput at boot so /dev/uinput carries its udev
# permissions and logind's uaccess ACL from the first login on.
# Installed as /etc/modules-load.d/w11-uinput.conf by
# gnome/install-bridge.sh --udev.
uinput
